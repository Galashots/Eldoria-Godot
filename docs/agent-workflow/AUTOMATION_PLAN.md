# Automation Plan

**Historical.** The milestones below predate this doc set and are all long since complete
— kept for the workflow pattern, not as a current to-do list. For what's actually next, see
`docs/CURRENT_STATE.md` ("Next milestone") and `docs/design/NORTH_STAR.md`.

Codex or Antigravity may implement one small vertical slice per task.

Every task must read `AGENTS.md` and `docs/CURRENT_STATE.md`, inspect only relevant files, preserve the working slice, and update `docs/CURRENT_STATE.md` after meaningful changes.

## Near-term milestones

1. Movement and collision complete.
2. NPC, collectible, and objective prompt.
3. Dialogue box.
4. Quest completion loop.
5. Godot versus V2 comparison.

