# Armor Tier Progression

A future equipment system will let both heroes wear armor that visibly upgrades their
appearance. This doc locks the tier list and visual theme so future art generation passes
stay consistent. Tiers apply identically (same names, same relative power order) to both
Grade 2 Mage and Grade 5 Adventurer, but read differently per character: the Mage gets
light layered pieces over the existing robe (pauldrons, sash, circlet — mages don't wear
bulky plate), the Adventurer gets traditional escalating armor (vest, bracers, chest plate,
helm).

| # | Tier | Visual theme |
|---|------|--------------|
| 1 | Leather | humble starter gear — worn brown leather |
| 2 | Bronze | dull copper-orange metal, basic plates |
| 3 | Iron | sturdy practical gunmetal gray |
| 4 | Gold | shiny ornate ceremonial gold |
| 5 | Diamond | sparkling icy blue-white crystalline |
| 6 | Ninja | sleek black/indigo stealth garb (a stylistic side-branch, not a raw power step) |
| 7 | Dragon | deep red/orange, scaled, fire-touched |
| 8 | Cosmic | starry deep purple/blue, otherworldly |
| 9 | Dark | shadowy black/violet, ultimate tier — kept friendly, not scary, per `NORTH_STAR.md`'s tone |

Status: Tier 1 (Leather) art has been generated and normalized into full sprite sets for
both characters (`assets/sprites/characters/{mage,adventurer}_body_idle_tier1_*.png`).
Equip/unequip logic and UI do not exist yet — no quest or inventory system currently grants
armor, so the `Player.gd`/`Player.tscn` swap point for these sprites is not yet wired. See
`docs/CURRENT_STATE.md` for current status.

## Production approach

Armor art is generated by in-place-editing the existing idle body sheet (same technique
validated for the body art itself: attaching/referencing the existing character render and
asking for an edit that holds pose/proportions stable), producing a full "body wearing
armor" image per direction.

The original plan here was to diff each armored frame against the matching un-armored idle
source to extract a transparent-only overlay for the existing hidden `Armor`
`AnimatedSprite2D` layer. In practice, ChatGPT-returned armor sheets are not pixel-aligned
with their source idle sheets (different canvas sizes, slightly different framing/scale
each generation), so a raw diff would produce broken, haloed cutouts. Tier 1 instead went
through the pipeline unmodified as a **full replacement body sprite set** per tier, exactly
like the base idle/walk art — zero new pipeline code, reusing `tools/asset_pipeline/`
as-is. The tradeoff is more disk space per tier (a full sprite set instead of a small
transparent overlay) and the `Armor` node staying unused for body-level gear; it may still
be useful later for small decorative accessories (capes, masks, auras) that genuinely are
separable overlays. Future tiers should follow the same full-body-swap approach unless a
future generation proves reliably pixel-aligned enough to revisit diffing.
